<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:iconPacks="clr-namespace:SecurigeIconPack"
    xmlns:converter="clr-namespace:SecurigeIconPack.Core.Converter;assembly=SecurigeIconPack.Core">
    <!--<Style TargetType="{x:Type local:SecurigeIcons}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:SecurigeIcons}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/SecurigeIconPack;component/Themes/SecurigeIconPack.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type iconPacks:SecurigeIcons}" BasedOn="{StaticResource SecurigeControl.Styles.SecurigeIconPack}" />

</ResourceDictionary>
